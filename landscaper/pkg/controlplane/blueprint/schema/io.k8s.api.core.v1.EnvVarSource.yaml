description: EnvVarSource represents a source for the value of an EnvVar.
properties:
  configMapKeyRef:
    $ref: blueprint://schema/io.k8s.api.core.v1.ConfigMapKeySelector.yaml
    description: Selects a key of a ConfigMap.
  fieldRef:
    $ref: blueprint://schema/io.k8s.api.core.v1.ObjectFieldSelector.yaml
    description: 'Selects a field of the pod: supports metadata.name, metadata.namespace,
      `metadata.labels[''<KEY>'']`, `metadata.annotations[''<KEY>'']`, spec.nodeName,
      spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.'
  resourceFieldRef:
    $ref: blueprint://schema/io.k8s.api.core.v1.ResourceFieldSelector.yaml
    description: 'Selects a resource of the container: only resources limits and requests
      (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory
      and requests.ephemeral-storage) are currently supported.'
  secretKeyRef:
    $ref: blueprint://schema/io.k8s.api.core.v1.SecretKeySelector.yaml
    description: Selects a key of a secret in the pod's namespace
type: object
