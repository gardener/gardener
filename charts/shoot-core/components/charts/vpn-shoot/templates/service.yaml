---
apiVersion: v1
kind: Service
metadata:
  annotations:
{{ include "utils-templates.reconcile-warning-annotation" . | indent 4 }}
  name: vpn-shoot
  namespace: kube-system
  labels:
    app: vpn-shoot
spec:
  selector:
    app: vpn-shoot
  type: LoadBalancer
  ports:
  - name: openvpn
    protocol: TCP
    port: 4314
    targetPort: 1194
