apiVersion: v1
kind: ServiceAccount
metadata:
  annotations:
{{ include "utils-templates.reconcile-warning-annotation" . | indent 4 }}
  name: vpn-shoot
  namespace: kube-system
  labels:
    app: vpn-shoot
