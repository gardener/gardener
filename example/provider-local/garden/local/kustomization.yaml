apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
- controllerdeployment.yaml
- controllerregistration.yaml

patches:
# patch-controllerdeployment-prow.yaml must be enlisted first because it contains an empty values map when running in a non-prow environment.
- path: patch-controllerdeployment-prow.yaml
- path: patch-imagevector-overwrite.yaml
