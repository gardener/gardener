apiVersion: operator.gardener.cloud/v1alpha1
kind: Garden
metadata:
  name: local
  annotations:
    gardener.cloud/fast-deployment: "true"
spec:
  runtimeCluster:
    provider:
      zones:
        - "0"
