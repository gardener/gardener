apiVersion: garden.sapcloud.io/v1beta1
kind: SecretBinding
metadata:
  name: core-openstack
  namespace: garden-development
  labels:
    cloudprofile.garden.sapcloud.io/name: openstack
secretRef:
  name: core-openstack
# namespace: another-namespace // optional
# quotas:
# - name: quota-1
# # namespace: another-namespace // optional
