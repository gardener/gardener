apiVersion: networking.istio.io/v1
kind: Gateway
metadata:
  name: metrics
  namespace: {{ .Release.Namespace }}
spec:
  selector:
{{ toYaml .Values.labels | indent 4 }}
  servers:
  - hosts:
    - '*'
    port:
      name: metrics
      number: 15022
      protocol: HTTP